import graphics.py as gr
import playGround.py as pg

for i in pg.grid[0].len():
  for j in pg.grid[i].len():
    if(pg.grid[i][j] != false):
      pg.grid[i][j].x = x
      pg.grid[i][j].y = y
      point = gr.Point(x,y)
      point.setfill(color_rgb(255, 0, 0))
